general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = hyprlock
    after_sleep_cmd = hyprctl dispatch dpms on # && ~/.config/hypr/apps/swaync/run.sh
}
}

listener {
    timeout = 580  # 9m 40s - lock screen
    on-timeout = hyprlock
}

listener {
    timeout = 600  # 10m - display off
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on # && ~/.config/hypr/apps/swaync/run.sh
}

# Note: Intentionally NO suspend/sleep listener